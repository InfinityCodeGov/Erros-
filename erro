pluginManagement {
    repositories {
        google() // Adicionado para garantir que o plugin Android seja encontrado
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "StudyTask"
include(":app")
